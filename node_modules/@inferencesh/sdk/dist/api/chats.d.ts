import { HttpClient } from '../http/client';
import { ChatDTO as Chat, ChatTraceDTO, CursorListRequest, CursorListResponse } from '../types';
/**
 * Chats API
 */
export declare class ChatsAPI {
    private readonly http;
    constructor(http: HttpClient);
    /**
     * List chats with cursor-based pagination
     */
    list(params?: Partial<CursorListRequest>): Promise<CursorListResponse<Chat>>;
    /**
     * Get a chat by ID
     */
    get(chatId: string): Promise<Chat>;
    /**
     * Update a chat
     */
    update(chatId: string, data: Partial<Chat>): Promise<Chat>;
    /**
     * Delete a chat
     */
    delete(chatId: string): Promise<void>;
    /**
     * Get chat trace (for debugging/observability)
     */
    getTrace(chatId: string): Promise<ChatTraceDTO>;
}
export declare function createChatsAPI(http: HttpClient): ChatsAPI;
