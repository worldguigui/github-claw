import { HttpClient } from '../http/client';
import { File, CursorListRequest, CursorListResponse } from '../types';
export interface UploadFileOptions {
    filename?: string;
    contentType?: string;
    path?: string;
    public?: boolean;
}
/**
 * Files API
 */
export declare class FilesAPI {
    private readonly http;
    constructor(http: HttpClient);
    /**
     * List files with cursor-based pagination
     */
    list(params?: Partial<CursorListRequest>): Promise<CursorListResponse<File>>;
    /**
     * Get a file by ID
     */
    get(fileId: string): Promise<File>;
    /**
     * Delete a file
     */
    delete(fileId: string): Promise<void>;
    /**
     * Upload a file (Blob or base64 string)
     */
    upload(data: string | Blob, options?: UploadFileOptions): Promise<File>;
    /**
     * Process input data and upload any files (Blobs, base64 strings)
     * Returns the processed input with file URIs replacing file data
     */
    processInput(input: unknown, path?: string): Promise<unknown>;
}
export declare function createFilesAPI(http: HttpClient): FilesAPI;
